# Mercury 跨平台复刻 — Stage 3 ~ v2.0.0 功能总结

> 供汇报使用 | 2026-07-26

---

## 项目概述

Mercury RSS 阅读器的跨平台复刻，基于 Rust + Tauri 2 + React + TypeScript + SQLite，支持 Windows 10+ / macOS 12+ / Linux。

---

## 一、AI 功能（Stage 3）

### 1. AI 摘要

- OpenAI 兼容协议，支持 DeepSeek / OpenAI / Ollama / Groq / Kimi 等主流服务
- 流式输出（SSE），实时显示生成进度，不卡 UI
- 可配置目标语言与详细程度（简洁 / 均衡 / 详细）
- Prompt 模板可自定义（YAML 资源文件）
- 摘要结果持久化到 SQLite，切换文章不丢失

### 2. AI 双语翻译

- **阅读页内嵌模式**：工具栏一键切换，不离开阅读页，无需切换 Tab
- 段落级双语对照布局（左原文 / 右译文），CSS Grid 双栏
- 多段落并发翻译（默认 3 段，可配 1~5），tokio Semaphore 控制
- **流式实时显示**：排队中 → 翻译中（逐字显示）→ 完成，用户无需等待
- **中文文章智能识别**：CJK 字符 >30% 自动跳过 LLM，原文即译文，不浪费 token
- 失败段落可单独重试
- 标题 + 作者合成前置段落，翻译时保持信息对齐
- 支持「回到原文」「清除翻译」

### 3. AI Provider 管理

- 多 Provider 配置（名称、Base URL、API Key、默认模型）
- 连接验证：发送最小请求测试连通性，网络异常不误报
- **API Key 加密存储**：XOR + 机器绑定密钥派生，非明文落库
- 支持同时配置多个 Provider，一键切换默认

### 4. AI 标签推荐

- 阅读页标签面板打开时**自动触发**，无需手动点击
- 输入文章标题 + 摘要 → LLM 分析 → 返回 JSON 数组标签建议（3-5 个）
- 60 秒超时，**失败静默降级**，不影响用户体验
- 注入已有标签库词汇作为参考，引导 LLM 优先复用现有标签
- NLP 本地兜底：从标题提取大写实体词作为建议，AI 不可用时仍可用
- AI 建议（蓝色 chip）排在本体建议（灰色 chip）前面

---

## 二、标签系统（Stage 4/5）

### 1. 文章打标签

- 阅读页工具栏 `#` 按钮 → 弹出标签面板
- 手动输入创建标签，大小写不敏感匹配已有标签
- 点击已有标签或建议标签一键添加/移除
- 侧边栏标签列表实时刷新
- 点面板外任意位置自动关闭

### 2. 侧边栏标签筛选

- 侧边栏分段切换：订阅源 / 标签
- 标签列表：颜色圆点 + 名称 + 使用次数
- 多选标签筛选文章（Set 存储）
- OR / AND 匹配模式切换
- 标签搜索（实时过滤，大小写不敏感）
- 右键菜单：重命名、删除标签
- 切回订阅源自动清除标签筛选 + 恢复文章列表

### 3. 标签库管理

- 设置 > 标签：完整标签管理界面
- 搜索 / 过滤（全部 / 临时 / 未使用 / 有别名 / 潜在重复）
- 标签详情：名称、颜色、使用次数、创建时间
- 别名管理：添加 / 删除别名
- 合并标签：将标签 A 的所有文章合并到标签 B
- 重复检测：复数变体 / 命名变体 / 拼写变体自动识别
- 批量删除未使用标签

### 4. 批量标签（AI 批量分析）

- 设置 > 批量标签
- 选择时间范围（1周~1年 / 全部 / 未读）
- 可选跳过已有标签的文章
- 并发度可配（1~5）
- 候选文章数实时查询数据库
- AI 批量分析 → 审查阶段（保留/丢弃建议标签）→ 应用

---

## 三、Token 用量统计（v2）

- 每次摘要 / 翻译 / 标签推荐完成后**自动记录**到 `llm_usage_events` 表
- 用量面板：设置 > 用量
- 时间窗口切换（7/14/30/90 天）
- 任务过滤（全部 / 摘要 / 翻译）
- 每日折线图 + 统计卡片（总计 / 输入 / 输出 / 请求数 / 成功率 / 平均）
- Provider / Model / Agent 多维度对比
- 数据保留天数可配（30/90/180/365/永久）
- 支持清除过期、清除全部

---

## 四、多语言 i18n（v2）

- 中英文 UI 切换，设置 > 外观 > 语言下拉，即时生效无需重启
- 默认跟随操作系统语言
- 320 条翻译覆盖全部界面文字、提示词、按钮
- 所有面向用户字符串走 `t()` 函数，禁止硬编码
- 时间相对显示也支持双语（刚刚/分钟前/小时前/天前 → just now/min ago/hr ago/d ago）
- Agent 输出语言（摘要/翻译目标语言）与 UI 语言独立设置

---

## 五、其他重要修复与优化

### 性能与稳定性

- **Feed 添加/刷新全链路异步化**：移除 `block_on` + 嵌套 runtime，不再阻塞主 UI
- **刷新崩溃修复**：清理 `refresh_all_feeds` 中残留的嵌套 tokio runtime
- 流式翻译字符重复修复（React Strict Mode listener 泄漏）
- Provider 验证状态持久化（localStorage，刷新不丢失）
- 翻译跨文章串数据修复（translationEntryRef 拦截过期回调）

### 内容解析

- **相对 URL 解析**：`/posts/xxx` 自动基于 feed URL 拼接为完整地址
- 重复订阅源错误提示中文化
- Toast 错误提示显示时长 3s → 6s

### 交互体验

- 点击标签面板外自动关闭
- 切回订阅源自动恢复文章列表 + 清除阅读区
- 标签添加后侧边栏实时刷新
- 阅读器组件按 `selectedEntry.id` 作为 key，切文章完整重建避免状态残留

---

## 六、技术指标

| 指标 | 数值 |
|---|---|
| Rust 测试 | 94 passed, 0 failed |
| TypeScript 编译 | 零错误 |
| 支持平台 | Windows / macOS / Linux |
| 数据库迁移 | 8 个版本化迁移（不可回退） |
| 翻译覆盖率 | 320 条中英双语 |
| 支撑 Provider | DeepSeek / OpenAI / Ollama / Groq / Kimi 等 |

---

## 七、版本标签

| 版本 | 内容 |
|---|---|
| `v1.0.0` | Feed 订阅、阅读体验、AI 摘要、双语翻译、笔记导出 |
| `v2.0.0` | 标签系统、Token 用量、多语言 i18n、UI 设计系统 |
